# 🏃‍♂️ دليل تشغيل مشروع Skill-Match خطوة بخطوة

هذا الملف يشرح كيفية تشغيل المشروع من الصفر على جهازك.

---

## 📋 المتطلبات المسبقة

قبل البدء، تأكد من تثبيت البرامج التالية:

1. **[Node.js](https://nodejs.org/)** (الإصدار 18 أو أحدث)
   - للتحقق: `node --version`
2. **[MongoDB](https://www.mongodb.com/try/download/community)** (أو حساب Atlas)
   - للتحقق: `mongod --version`

---

## 🛠️ خطوات التشغيل

### 1. تثبيت المكتبات
افتح التيرمينال في مجلد المشروع ونفذ الأمر التالي لتثبيت كل المكتبات المطلوبة:

```bash
npm install
```

### 2. إعداد متغيرات البيئة (.env)
المشروع يحتاج لملف `.env` يحتوي على الإعدادات السرية.

1. انسخ ملف المثال المرفق:
   ```bash
   # في Windows PowerShell
   Copy-Item .env.example .env
   ```
2. افتح ملف `.env` وعدل القيم (إذا كنت تستخدم MongoDB محلي، القيم الافتراضية تعمل جيداً):
   ```ini
   MONGODB_URI=mongodb://127.0.0.1:27017/skill_match_db
   PORT=5000
   JWT_SECRET=myStrongSecretKey123
   ```

### 3. تشغيل قاعدة البيانات (MongoDB)
*تخطى هذه الخطوة إذا كنت تستخدم MongoDB Atlas.*

تأكد أن خدمة MongoDB تعمل على جهازك. في أغلب الأحيان تعمل تلقائياً بعد التثبيت كـ Service.
للتحقق من أنها تعمل، افتح MongoDB Compass وحاول الاتصال بـ `127.0.0.1:27017`.

### 4. تشغيل السيرفر
الآن شغل السيرفر بالأمر:

```bash
node server.js
```

إذا ظهرت لك الرسالة التالية، فالمشروع يعمل بنجاح:
```
✅ MongoDB Connected Successfully!
🚀 Server running on port 5000
```

---

## 🧪 اختبار المشروع

للتأكد من أن كل شيء يعمل (التسجيل، تسجيل الدخول، القواعد)، قمنا بتجهيز سكربت اختبار آلي.
في نافذة تيرمينال جديدة (اترك السيرفر يعمل)، شغل:

```bash
node test_complete.js
```

ستظهر لك نتائج الاختبارات (علامات صح خضراء ✅) إذا كان كل شيء سليم.

---

## 📚 التوثيق (API Documentation)

لديك خياران لاستعراض التوثيق:

1. **Swagger UI** (تفاعلي):
   🔗 **[http://localhost:5000/api-docs](http://localhost:5000/api-docs)**

2. **ReDoc** (تصميم حديث للقراءة):
   🔗 **[http://localhost:5000/redoc](http://localhost:5000/redoc)**

هنا ستجد تفاصيل كل الـ Endpoints وطريقة استخدامها.

---

## 🐛 حل المشاكل الشائعة

**مشكلة: `connect ECONNREFUSED 127.0.0.1:27017`**
- **السبب:** MongoDB غير شغال على جهازك.
- **الحل:** شغل برنامج MongoDB Compass وتأكد من الاتصال، أو شغل خدمة MongoDB من الـ Services في الويندوز.

**مشكلة: `EADDRINUSE: address already in use :::5000`**
- **السبب:** المنفذ 5000 مشغول ببرنامج آخر (ربما نسخة قديمة من السيرفر).
- **الحل:** غير الـ `PORT` في ملف `.env` إلى 5001 أو أي رقم آخر.
