# 🟣 دليل اختبار Skill-Match API باستخدام Insomnia

هذا الدليل يشرح كيفية استيراد جميع الـ Endpoints الخاصة بالمشروع إلى برنامج **Insomnia** وكيفية اختبارها خطوة بخطوة.

---

## 📥 الخطوة 1: استيراد المشروع (Import)

أسهل طريقة هي استيراد ملف Swagger الذي أنشأناه تلقائياً.

1. افتح مشروع Skill-Match وتشغل السيرفر (`node server.js`).
2. افتح برنامج **Insomnia**.
3. اضغط على زر **Create** (أو **+**) ثم اختر **URL**.
4. انسخ وألصق الرابط التالي:
   ```
   http://localhost:5000/api-docs.json
   ```
5. اضغط **Fetch and Import**.
   - ستظهر لك رسالة "Import Succeeded" وقائمة بكل الـ Requests.

---

## ⚙️ الخطوة 2: إعداد التوكن (Environment Setup)

بما أن معظم العمليات تحتاج تسجيل دخول، سنقوم بإعداد متغير (Variable) للتوكن ليسهل استخدامه.

1. اضغط على **Manage Environments** (الترس ⚙️ أو `No Environment`).
2. في ملف `Base Environment`، أضف الكود التالي:
   ```json
   {
     "base_url": "http://localhost:5000/api",
     "token": "سنضع_التوكن_هنا_لاحقا"
   }
   ```
3. اضغط **Done**.

---

## 🧪 الخطوة 3: سيناريو الاختبار الكامل

اتبع هذا الترتيب لاختبار المشروع بشكل منطقي:

### 1️⃣ تسجيل حساب جديد (Register)
- اذهب إلى مجلد **Auth** -> **Register a new user**.
- في الـ **Body** (JSON)، غيّر البيانات لتكون جديدة:
  ```json
  {
    "name": "Momen Motaz",
    "email": "momen@test.com",
    "password": "123"
  }
  ```
- اضغط **Send**.
- ✅ **النجاح:** ستصلك رسالة `User registered successfully`.

### 2️⃣ تسجيل الدخول (Login)
- اذهب إلى **Auth** -> **Login user**.
- استخدم نفس الإيميل والباسورد:
  ```json
  {
    "email": "momen@test.com",
    "password": "123"
  }
  ```
- اضغط **Send**.
- ✅ **النجاح:** ستحصل على `token` طويل.
- 📋 **انسخ هذا التوكن (بدون علامات التنصيص "").**

### 3️⃣ حفظ التوكن في البيئة
- ارجع إلى **Manage Environments**.
- الصق التوكن مكان `سنضع_التوكن_هنا_لاحقا`.
- اضغط **Done**.

### 4️⃣ إضافة مهارة (Add Skill) - *يحتاج توكن*
- اذهب إلى **Skills** -> **Add a new skill**.
- اذهب إلى تبويب **Auth** واختر **Bearer Token**.
- في خانة Token اكتب: `{{ token }}` (سيتحول لونها للبنفسجي).
- في الـ **Body**:
  ```json
  {
    "name": "Node.js"
  }
  ```
- اضغط **Send**.

### 5️⃣ عرض المهارات (Get Skills)
- اذهب إلى **Skills** -> **Get all skills**.
- اضغط **Send**. ستظهر لك المهارة التي أضفتها.

### 6️⃣ إنشاء ملفك الشخصي (Update Skills)
- اذهب إلى **Users** -> **Update user skills**.
- **Auth**: اختر Bearer Token واكتب `{{ token }}`.
- **Body**: (تحتاج هنا لـ IDs المهارات التي حصلت عليها من الخطوة 5):
  ```json
  {
    "skills": ["675xxxxxxxxxxxxxxxxxxx"]
  }
  ```
- اضغط **Send**.

### 7️⃣ إنشاء وظيفة (Create Job)
- اذهب إلى **Jobs** -> **Create a new job**.
- **Auth**: Bearer Token -> `{{ token }}`.
- **Body**:
  ```json
  {
    "title": "Backend Developer",
    "description": "We need a Node.js expert",
    "skills": ["Node.js"]
  }
  ```
- اضغط **Send**.

### 8️⃣ المطابقة (Matching)
- اذهب إلى **Matching** -> **Get matching jobs**.
- **Auth**: Bearer Token -> `{{ token }}`.
- اضغط **Send**.
- ✅ **النتيجة:** يجب أن تظفر لك الوظيفة التي أنشأتها لأنك تملك مهارة "Node.js"!

---

## 💡 نصيحة للمحترفين

بدلاً من نسخ التوكن يدوياً في كل مرة، يمكنك استخدام ميزة **Chaining** في Insomnia:
1. في خانة التوكن، اكتب `Response => Body Attribute`.
2. اضغط عليها واضبط الإعدادات:
   - **Request**: Login user
   - **Filter**: $.token
3. الآن التوكن سيتحدث تلقائياً كلما عملت Login!
